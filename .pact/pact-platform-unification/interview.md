# 访谈门（Interview Gate）

> 创建日期: 2026-08-11
>
> 原则：**猜错的代价 > 一次往返** 才阻塞提问；否则取默认 + 标注「假设」继续。
> 阻塞项**批量**用 `AskUserQuestion` 一次问清，别一个个挤牙膏。
> 未决阻塞不得进入阶段 4（写 Contracts）。

## 十二类逐项过（每类：要么问、要么取默认并标注）

| # | 类别 | 状态 | 结论 / 假设 |
|---|---|---|---|
| 1 | 交付形态（Web/小程序/CLI/服务/库，跑在哪，谁部署） | 已答 | PACT Platform monorepo：继续独立发布 PACT skills、`@vima-tech/ui-admin` 与 `create-vima-starter`；由框架维护者在本地/CI 构建发布 |
| 2 | 用户与角色（几种人、权限是否分层、谁看谁的数据） | 已答 | 框架维护者、发布维护者、AI Coding Agent、生成项目接管者；本期无业务 RBAC，代码写权限由 Git/CI 管理 |
| 3 | 核心场景（最重要的三条旅程，逐步骤） | 已答 | ①能力发现与选择；②旧多仓快照迁入且旧路径兼容；③按变更计算发布单元并串行验证/独立发布 |
| 4 | 规模与性能（数据量级、并发、响应上限） | 假设 | 首期 3 个发布单元、2 个 Vima Adapter、3 个 Delivery Profile；本地全量验证串行，单项退出码和报告比耗时优先 |
| 5 | 数据与持久化（存哪、什么形态、兼容既有 schema？迁移？） | 已答 | 全部治理数据使用 Git 内 JSON/Markdown；不引入数据库；旧绝对路径由符号链接兼容，包名和 CLI 名保持不变 |
| 6 | 集成与外部依赖（对接什么系统、谁给凭据） | 已答 | Git、Node/npm、bash、Maven/Java；发布凭据仍由外部 CI/维护者提供，本迁移不写入凭据也不实际 npm publish |
| 7 | 权限与合规（个人信息/未成年人/金融/医疗？脱敏与审计） | 假设 | 不处理个人信息；重点是文件路径边界、命令白名单、供应链与发布 dry-run；release plan 必须可审计 |
| 8 | **非目标（明确不做什么）** ← 跳过代价最大 | 已答 | 不合并成一个 npm 包；不让 PACT Core 强依赖 Vima；不改变生成项目运行时依赖；不删除独立发布身份；不在本次执行真实外部发布 |
| 9 | 成功定义（可量化的一句话） | 已答 | 三阶段全部落地：治理 Schema/矩阵有效；两个产品位于 PACT repo 且旧路径可用；changed release plan 与全量 verify 可执行，全部应通过的门通过 |
| 10 | 约束（技术栈是否指定、团队会什么、deadline、禁用依赖） | 已答 | 保持 bash/Node/npm/Maven/Vue/Spring 技术栈；不新增重型 monorepo 工具；不改公开包名；用户要求连续做到三阶段完成 |
| 11 | 视觉与交互（有设计稿吗？URL/.dc.html/截图；没有按什么风格） | 已答 | 迁移不重设计现有 UI；文档/CLI 输出保持现有风格；ui-admin 继续遵守 Manifest、SVG VIcon、无 Emoji 功能图标 |
| 12 | 既有资产（要复用/兼容/替换的存量代码、数据、接口） | 已答 | 复用 pact skill 全仓、ui-admin 当前源码与测试、starter 当前 frontend/backend/cli；两个 Vima 目录无独立 Git 历史，记录导入快照哈希和原路径 |

## OPEN 问题清单

| OPEN-Q | 类别 | 描述 | 判定 | 处置 | 答复 |
|---|---|---|---|---|---|
| Q001 | 4 规模 | 后续发布单元数量未知 | 可假设 | 首期按 3 个发布单元设计，registry/schema 可扩展 | 假设已采用 |
| Q002 | 7 安全 | 外部 registry 发布凭据与 CI 平台未指定 | 可假设 | 只生成 release plan 与独立发布命令，不执行真实 publish | 假设已采用 |
| Q003 | 12 资产 | ui-admin/starter 无独立 `.git`，无法导入原 Git 历史 | 可假设 | 保存当前快照、来源路径、导入时间与内容哈希；PACT repo 从导入提交开始保留历史 | 假设已采用 |

## 提问草稿（喂给 AskUserQuestion，每问 2–4 个候选含推荐项）

无阻塞问题。用户已接受此前推荐的“同仓不同边界、独立发布、按需启用”并要求三阶段全部完成。

## 结论

> 十二类全部已答或显式假设，OPEN 无阻塞。访谈门通过，进入多来源熔合。
